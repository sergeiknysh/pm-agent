# PM-as-Files

> Управление проектами, которое живет в файловой системе. Local-first, Git-friendly, Hackable.

## 🚀 Что такое PM-as-Files
Это система управления задачами, где **источником правды являются файлы**.
Вместо того чтобы прятать задачи в облачную базу данных (как Jira, Trello или Notion), PM-as-Files хранит каждую задачу как отдельный **Markdown-файл** на твоем диске. Поверх этих файлов работает умный движок (Node.js + React), который превращает папки с текстом в удобную Канбан-доску и API.

## 📂 Почему Файлы, а не База Данных?

Выбор файловой системы вместо SQL/NoSQL — это осознанное архитектурное решение (Local-first подход):

1.  **Полный контроль (No Vendor Lock-in):**
    Твои данные — это просто текст. Если завтра это веб-приложение исчезнет, ты всё равно сможешь открыть свои задачи в Obsidian, VS Code, Notepad или Vim. Ты не зависишь от экспорта данных из проприетарного облака.

2.  **Git — это твоя машина времени:**
    Храня задачи в файлах, ты получаешь всю мощь Git бесплатно: историю изменений, версии, ветки, бэкапы и синхронизацию между устройствами. `git log` покажет, как менялась задача, лучше любого "Activity Log" в Jira.

3.  **Универсальный доступ (Hackability):**
    Ты можешь править задачи через красивый Web UI, а можешь — массово менять статусы через `sed` в терминале, искать через `grep` или редактировать скриптами на Python/Bash. Система открыта для любой автоматизации.

4.  **Простота и прозрачность:**
    Одна задача = Один файл. Это делает структуру понятной человеку. Метаданные (статус, срок) лежат в шапке файла (YAML), а описание — в теле. Это самый надежный и долговечный формат хранения знаний.

---

## 🏗 Структура проекта

- **`pm-as-files/`** (этот репозиторий): Движок системы. Backend (Node.js/Fastify) и Frontend (React/Vite).
- **`pm/`**: Данные. Папка, где лежат ваши задачи, проекты и входящие заметки. Она монтируется в приложение.

Полная документация и спецификации лежат в [pm/docs/](pm/docs/README.md).

---

## ⚡️ Quick Start (Dev)

Требования: Docker Desktop / Docker Engine + Compose.

Запуск backend + frontend с hot-reload:
```bash
make up
# или
docker compose up --build
```

- **Web UI**: http://localhost:5173
- **API**: http://localhost:8787

Остановка:
```bash
make down
```

Логи:
```bash
make logs
```

### Персистенция данных
- Папка `./pm` прокидывается внутрь контейнера. Все созданные задачи сохраняются на диске хоста.
- Папка `./.pm-secrets` хранит сессии и хеши паролей.
